[package]
name = "asciicam"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
nokhwa = { version = "0.10", features = ["input-native"] }
image = { version = "0.25", features = ["default-formats"] }